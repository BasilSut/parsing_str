#include "Tokenizer.hpp"

namespace simpleparser{
    using namespace std;
    vector<Token> Tokenizer::parse(const string &inProgram);
    vector<Token> tokens;
    Token currentToken;
    for(currCh : inProgram){
        switch(currCh){
            case '0':
            case '1':
        }
    }
}
